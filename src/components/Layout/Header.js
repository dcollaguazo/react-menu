import React from "react";

const Header = (props) => {
  return;
  <React.Fragment>
    <header>
      <h1>ReactMeals</h1>
    </header>
    <button>Cart</button>
    <div>
      <img />
    </div>
  </React.Fragment>;
};

export default Header;
